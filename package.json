{
  "name": "babylonplanets",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Standalone Babylon.js map / planet / skybox builder that exports Space Arena arena configs",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@babylonjs/core": "^8.0.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
